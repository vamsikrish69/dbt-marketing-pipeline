
name: 'my_new_project'
version: '1.0'
profile: 'default'

model-paths: ["models"]

models:
  my_new_project:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
