
version: 2

models:
  - name: fct_campaign_performance
    columns:

      - name: campaign_id
        tests:
          - not_null

      - name: source_name
        tests:
          - accepted_values:
              arguments:
                values: ['meta', 'google']

      - name: roas
        tests:
          - not_null
